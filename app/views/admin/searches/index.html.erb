<div class ="container">
  <div class ="row">
      <% if @range =="ユーザー名"%>
        <h2>検索結果</h2>
          <div class="col-md-12">
            <table class="table">
              <thead>
                <tr>
                  <th>プロフィール画像</th>
                  <th>名前</th>
                  <th></th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% if @customers.any? %>
                  <% @customers.each do |customer| %>
                    <tr>
                      <td>
                        <%= image_tag customer.get_profile_image, size: '50x50' %>
                      </td>
                      <td><%= customer.name %></td>
                      <td>
                        <% if customer.is_deleted == false %>
                          有効
                        <% else %>
                          退会
                        <% end %>
                      </td>
                      <td><%= link_to "ユーザー詳細", admin_customer_path(customer), class: "btn btn-primary" %></td>
                    </tr>
                  <% end %>
                <% else %>
                  <tr>
                    <td colspan="3">ユーザーがいません</td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
      <% else %>
        <h2>検索結果</h2>
        <div class="row row-cols-1 row-cols-md-3 g-4">
          <%= render 'layouts/admin_reviews', posts: @posts %>
          <%= paginate @posts, theme: 'twitter-bootstrap-4' %>
          </div>
        </div>
      <% end %>
  </div>
</div>